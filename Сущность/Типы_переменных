                            ~~Типы перменных~~

Типы: => определяется типом присвоенного значения переменной

    undefined - значение не определено, его нет
    const a = 10    //INT 
    const b = 'abc' //STRING

Примитивные типы: => Содержат значение непосредственно в памяти
    string
    boolean
    number
    null - явное указание того, что у переменной отсутствует значение, либо будет объявлено позднее
    undefined - пустое значение, присваивается автоматически при объявлении переменной бех присваивания ей определенного значения
    symbol - уникальное значение

Ссылочный тип: => переменная будет хранить ссылку на объект в памяти пк, а не конкретное значение
    object - объект
        название : значение

Пример ссылочного объекта:
    const objectA = { // создание объекта с полями
    a : 10,
    b : true
    }

    const copyOfA = objectA // копируем ссылку в другую переменную

    copyOfA.a = 20 // изменение значения
    copyOfA.c = 100 // добавление переменной в объект

Динамическая типизация: => не указывается явно тип
    a = 'abc'
    a = 10 

Нужно стараться использовать const в объявлении переменных 
