                    ~~Базовая информация~~

Сущность в JS => это объект 

Объект => это набор свойств или пар ( { имя : значение : {} } ) - 
    могут быть вложенные объекты с своими полями

Массив, Фукнция, Число, Строка - это объект

Строка ведет себя как объект

'' "" - без разницы какие скобки

console.log('Hello World')  - вызов функции - выражение, потому что функция 
    всегда возвращает какое-то значение
    объект.метод('Значение')

log вернет undefined в консоль как результат

console.dir() - отображение свойств объекта
console.table() - все свойства объекта, который передан в аргумент в виде таблицы