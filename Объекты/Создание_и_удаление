                        ~~Объекты~~

Объект => это набор свойств или пар ( { имя : значение : {} } ) - 
    могут быть вложенные объекты со своими полями

Объекты: => набор свойств имя : значения - тип переменных ссылочный тип, 
    содержит ссылку на объект

Свойства объекта: => 
    свойство объекта - это поля (не функции)

Пример объекта => 
    const myCity = {
        city : 'New Tork',
        popular : true,
        country : 'USA'
    }

Доступ к значениям свойства объекта:

    console.log(myCity.city)
    myCity.city = 'Las Vegas' // перезапись
    myCity.member = 'Devid' // добавление свойства 
    myCity['popular'] = true // добавление новых свойств через квадратные скобки (удобно для написание имен полей, одинаковых с именами некоторых переменных)
    delete myCity.country // удаление свойств 

Вложенные объекты: 

    const myCity = { 
        city : 'New York',
        info : {
            isPopular : true, 
            country : 'USA'
        }
    }

    console.log(myCity.info.country) // доступ через 2 точки

Сокращенная запись:

    const name = 'Xenia'

    const userProfile = {
        postsQty
        sername,
        name : name
    }