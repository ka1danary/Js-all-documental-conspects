if в функциях 

const sumPositiveNumber = (a, b) => {
    if ( typeof a !== 'number' || typeof b !== 'number') {
        return 'Один из агрументов не число'
    }

    if (a <= 0 || b <= 0) {
        return 'Number are not positive'
    }

    return  a + b
}

console.log(sumPositiveNumber(0, 4))