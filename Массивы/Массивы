Масиивы - объект (__proto__ : Object)
 
    const myArray = [1, 2, 3]
    const myArray2 = new Array(1, 2, 3)

    myArray.length - длина массива

Каждый массив ссылается на разные места в памяти, даже если массивы одинаковые

    conole.log(myArray[0]) - обращение к конкретному элементу 