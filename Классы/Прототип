Что же такое прототип? __proto__ (скрытое свойство) 
благодаря нему и создается вся цепочка прототипов (когда идет наследование класс - объект - массив и тд.)

----------

(это примитивы) Строки и числа ведут себя как объекты, то есть у них тоже есть функции и свойства, как у объектов

 const myName = 'Xenia'
 myName.toUpperCase() // XENIA

и если мы создадим экземпляр класса String, то у него тоже есть все методы обычной переменной!!

const secondName = new String()
secondName.toUpperCase()