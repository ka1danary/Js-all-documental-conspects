/*
Создать функцию которая считает разницу между числами 
и с помощью js doc описать типы параматров,
функцию и возвращаемое значение
*/

/**
 * Возвращает разницу 2х чисел
 * @param {number} a 
 * @param {number} b 
 * @returns {number} Разница чисел
 */

const funck = (a, b) => {
    return (a >= b) ? (a - b) : (b - a)
}

const res = funck(3, 10)

console.log(res)